<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ProductMapper">

<!-- <select id="bestProduct" resultType="CategoryProductDTO">
select * from product order by  order by p_stock desc
</select>  -->


<select id="productList" resultType="CategoryProductDTO" parameterType="int">
	
	 
select p.P_ID
,(select 

IMAGE_ROUTE

from images
where p_id = p.p_id 
and IMAGE_RNK = 1) as p_image
,p.P_NAME
,p.P_CONTENT
,p.C_ID
,p.P_COST_PRICE
,p.P_SELLING_PRICE
,p.P_DISCOUNT
,p.P_CREATED
,p.P_STOCK
from product p

where p.c_id = #{c_id} 
	 
	 
	 
	 
</select>

<select id="productRetrieve" parameterType="int" resultType="ProductDTO">
	select * from product where p_id = #{p_id}
</select>	

<select id="ImagesRetrieve" parameterType="int" resultType="ImagesDTO">
select * from images where p_id= #{p_id}
</select>

<select id="searchProduct" resultType="CategoryProductDTO" parameterType="String">
	select p.P_ID,(select IMAGE_ROUTE from images where p_id = p.p_id and IMAGE_RNK = 1) as p_image
	, p.P_NAME, p.P_CONTENT, p.C_ID, p.P_COST_PRICE, p.P_SELLING_PRICE, p.P_DISCOUNT, p.P_CREATED, p.P_STOCK
	from product p
	where p_name like '%'||#{searchValue}||'%' or p_content like '%'||#{searchValue}||'%'
</select>

<insert id="addLike" parameterType="hashmap">
insert into product_liked values (#{userid},#{p_id})
</insert>

</mapper>
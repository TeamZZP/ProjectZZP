<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="AccountMapper">
	<update id="changePasswd" parameterType="hashmap">
		update member
		set passwd = #{chagnePasswd}
		where userid = #{userid}
	</update>
	<update id="changeEmail" parameterType="hashmap">
		update member
		set email1 = #{email1}, 
			email2 = #{email2}
		where userid = #{userid}
	</update>
	<update id="changeProfile" parameterType="hashmap">
		update profile
		set profile_txt = #{profile_txt}
		where userid = #{userid}
	</update>
	<select id="selectProfile" resultType="com.dto.ProfileDTO">
		select * from profile
		where userid = #{userid}
	</select>
	<update id="changeAddress" parameterType="hashmap">
		update address
		set post_num = #{post}, 
			addr1 = #{addr1}, 
			addr2 = #{addr2}
		where userid = #{userid}
	</update>
</mapper>
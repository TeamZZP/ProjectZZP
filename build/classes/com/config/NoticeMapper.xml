<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="NoticeMapper">
	<select id="pointNotice" resultType="NoticeDTO">
		select NOTICE_ID, NOTICE_TITTLE, NOTICE_CONTENT, to_char(NOTICE_CREATED, 'YYYY/MM/DD') NOTICE_CREATED, 
			ADMIN_ID, NOTICE_CATEGORY, NOTICE_HITS from notice where NOTICE_CATEGORY = 'main'
	</select>
	<select id="noticeOneSelect" resultType="NoticeDTO" parameterType="string">
		select NOTICE_ID, NOTICE_TITTLE, NOTICE_CONTENT, to_char(NOTICE_CREATED, 'YYYY/MM/DD') NOTICE_CREATED, 
			ADMIN_ID, NOTICE_CATEGORY, NOTICE_HITS from notice where NOTICE_ID = #{noticeID}
	</select>
	<update id="noticeUpdate" parameterType="NoticeDTO">
		update notice set NOTICE_TITTLE = #{NOTICE_TITTLE}, NOTICE_CONTENT = #{NOTICE_CONTENT} where NOTICE_ID = #{NOTICE_ID}
	</update>
	<delete id="NoticeDelete" parameterType="string">
		delete from notice where NOTICE_ID = #{NOTICE_ID}
	</delete>
</mapper>